# Copy to .env.local (for `npm run dev`) or set these in Netlify → Site configuration → Environment variables.

# Required: OAuth client ID from Google Cloud (see README → "Google sign-in setup").
VITE_GOOGLE_CLIENT_ID=

# Optional: the Google Sheet to open by default, so nobody has to paste the link.
VITE_SPREADSHEET_ID=
